<MudDrawer @bind-Open="Open">
    @* <HeaderTemplate>
        <HxSidebarBrand BrandName="Brand name" BrandNameShort="BN" />
    </HeaderTemplate> *@
    <MudNavMenu>
        <MudNavLink Href="/" Match="NavLinkMatch.All">Home</MudNavLink>
        <MudNavLink Href="/counter" Match="NavLinkMatch.Prefix">Counter</MudNavLink>
        <MudNavLink Href="/weather" Match="NavLinkMatch.Prefix">Weather</MudNavLink>
    </MudNavMenu>

    @* <FooterTemplate>
        <HxSidebarFooter Href="/login" Icon="BootstrapIcon.Person"
            Text="@(context.SidebarCollapsed ? null : "Login")" />
    </FooterTemplate> *@
</MudDrawer>

@code {
    private bool Open { get; set; } = true;
}